{
  "name": "library-status-tracker",
  "version": "1.0.0",
  "description": "A lightweight library open/closed status tracker with an activity log.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
